[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
# Ningun test de estos servicios necesita mas de 60s: si uno se cuelga, que
# falle rapido en vez de bloquear la suite entera.
timeout = 60
